#!/bin/bash
# ===== main.sh =====
# Menú principal del sistema automatizado del TIF

while true; do
  clear
  echo "═══════════════════════════════════════════════════════"
  echo "        🧩 Trabajo Final - Sistemas Operativos"
  echo "═══════════════════════════════════════════════════════"
  echo "1) Generar informe del sistema"
  echo "2) Realizar backup"
  echo "3) Limpiar archivos temporales"
  echo "0) Salir"
  echo "═══════════════════════════════════════════════════════"
  read -p "Elegí una opción: " op

  case $op in
    1) bash scripts/sysreport.sh ;;
    2) bash scripts/backup.sh ;;

    3) bash scripts/clean.sh ;;
    0) echo "👋 Saliendo..."; exit ;;
    *) echo "❌ Opción inválida"; sleep 1 ;;
  esac
done
